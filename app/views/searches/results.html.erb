<div class="result-page container">
  <%= render partial: 'form' %>

  <% @pages.each do |page| %>
    <div class="page-result">
      <%= link_to page.title, page.uri %>
      <p>
        <%= highlight (truncate page.content, length: 400), params[:qs] %>
      </p>
    </div>
  <% end %>

</div>
